# oh-my-boring — 이 저장소에서 일하는 방식

> **먼저 읽을 것**
> - [`docs/PRD.md`](./docs/PRD.md) — 제품 계약. §2 판정 계약과 §8 오너 결정이 정본이다.
> - [`drudge/CLAUDE.md`](./drudge/CLAUDE.md) — Rust 쪽 가드레일 (별도 SSOT, 여기 옮겨 적지 않는다).
> - `~/.claude/CLAUDE.md` — 전역 규율. 겹치면 전역이 이긴다. **여기에는 이 저장소에서만 참인 것만 적는다.**

이 파일이 생긴 이유: `wiki-1603` 의 소유자 결재 — *"전역 훅은 안 늘리고 저장소 게이트로, 못 잡는
것은 프로젝트 CLAUDE.md 절로."* 게이트로 잡히는 것은 아래에 적지 않는다. 적혀 있다는 것은
**기계가 못 잡는다**는 뜻이다.

## 1. 만들면 부른다

호출자 없는 함수·인자·게이트를 남기지 않는다. 2026-09-08~09 하루에 여섯 번 났다:

| 만든 것 | 부른 곳 |
|---|---|
| `verdict_core.coverage()` | 없었다 (#289 → #293) |
| `verdict_core.self_check_verdict()` | 없었다 (#289 → #295) |
| `uptake_core.sensitivity_probe()` | CLI 서브커맨드에만 (#292) |
| `coverage(automated_sessions=)` 인자 | 두 호출부 모두 안 채움 (#298) |
| `wiki-1636` 의 처방 절 | 주입 9회 중 0회 도달 (#299) |
| 폴링 차단 훅 | `exit 0` **뒤에** 붙어 죽은 코드 (원복) |

새 함수·인자·훅을 넣었으면 **같은 커밋에서 호출부를 보이고**, 라이브에서 한 번 돌려 값을 인용한다.
`bash -n` 과 임포트 성공은 호출 증거가 아니다.

## 2. 적으면 한다

"남은 일 / 미조치 / 따로 보겠다" 로 적은 것은 **그 자리에서 하거나, 안 할 이유를 적고 소유자에게
넘긴다.** 다음 결함으로 넘어가면서 조용히 두지 않는다.

2026-09-09 실측: 이 세션에서 그렇게 적어두고 안 한 것 — `.env.example` 누락 17개 · `CLAUDE.md`
부재 · ko/ja README 3주 정체 · claim 봉인행 48,233 · 간선 11,761 · 유령 정체항목 370.
승인받은 것은 했고 **스스로 적은 것은 거의 다 안 했다.** 그 비대칭이 이 절의 이유다.

## 3. 게이트는 저장소에

새 규율은 `scripts/guard.sh`·`scripts/doctor.sh`·`.github/workflows/ci.yml` 안에 세운다.
`~/.claude/hooks/` 는 늘리지 않는다 (소유자 결재, `wiki-1603`).

게이트를 올리기 전 **위반 변이와 통제군 둘 다** 돌려 red 를 본다. 통제군이 없으면 그 게이트는
"아무것도 안 잡는 것"과 구분되지 않는다. 2026-09-09: 폴링 게이트가 자기 시험 스크립트를 막았고,
그건 통제군을 돌렸기 때문에 알았다.

## 4. 0 을 데이터로 읽지 않는다

명령 실패·계기 부재·모집단 공백은 전부 0 으로 보인다. 이 저장소에서 그 셋을 구분하지 못해 난 사고:

- `pgrep -fc` 가 macOS 에 없어 폴백이 항상 0 → 살아 있는 워커를 죽었다고 선고, 24시간 방치
- `rg` 미설치를 커버리지 0% 로 읽음 (실제 81%)
- 처치·대조 동시 0 을 "채널 비작동"으로 선고 (탐지기 감도 미증명 상태였다, `PRD` §2)

부정형 단정("0건", "없다")은 **탐지기가 그것을 볼 수 있음을 먼저 보인 뒤**에만 쓴다.

## 5. 모집단을 밝힌다

비율을 낼 때 분자·분모가 무엇인지 같은 줄에 적는다. 2026-09-07: 같은 커버리지가 분모에 따라
14% 와 83% 였다 (`PRD` §8 D8). 표본을 코퍼스 사실로 인용한 사고가 이 세션에서만 다섯 번 났다.

## 6. 노드 완료는 산출물로 판정한다

`git log origin/main..HEAD | wc -l` · 파일 해시 · DB 행 수. 프로세스 생존(`pgrep`)이나 로그
꼬리(`tail`)는 판정 근거가 아니다 (`wiki-1636`).

대기는 `Monitor` 또는 `run_in_background` 로 한다 — 완료 통지가 곧 엣지다. `sleep` 뒤에 상태를
물어보는 것은 대기가 아니라 폴링이다. 2026-09-08 실측: 한 세션에서 `tail` 839회 · `sleep` 51회,
같은 세션이 그 전주에 `Monitor` 를 21회 썼다.

## 7. 배달까지가 일이다

머지는 끝이 아니다. `~/.hermes/scripts` 와 호스트 CLI 바이너리는 별도 아티팩트다.

- Python·훅 변경 → `python3 agents/shared/agent_wiring.py --install --boring-home "$PWD"`
- Rust 변경 → `make build` + 컨테이너 재기동, 호스트 CLI 는 `cargo build --release`
- 확인 → `sh scripts/doctor.sh` 가 ✗ 0 인지, 그리고 소비자 쪽에서 한 번 돌려 값을 인용
